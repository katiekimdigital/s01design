// PROTOTYPE #3 — "Results First". Non-destructive mock; live `/` untouched.
import { HeroResults } from '@/components/sections/HeroResults'

export default function LabResults() {
  return <HeroResults />
}
